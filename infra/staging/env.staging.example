# Staging Environment Configuration
# Copy this file to .env.staging and customize the values

# GitHub Container Registry
GITHUB_REPOSITORY=owner/repo
IMAGE_TAG=latest

# Domain and SSL
STAGING_DOMAIN=staging.memeit.pro
ACME_EMAIL=admin@memeit.pro

# MinIO (S3-compatible storage)
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=admin12345
S3_BUCKET=clips

# Application
ENV=staging
LOG_LEVEL=INFO

# Prometheus Basic Auth (required for /prometheus endpoint)
STAGING_PROM_USER=admin
STAGING_PROM_PASS=securepassword123

# Optional: Monitoring
PROMETHEUS_RETENTION=168h

# Optional: Custom ports (if needed)
# STAGING_PORT=80
# STAGING_SSL_PORT=443 